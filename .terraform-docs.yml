formatter: markdown

content: |-
  # terraform-aws-alb

  Terraform module to create an Application Load Balancer (ALB) and associated resources.

  ## Usage

  ### Basic

  ```hcl
  {{ include "examples/complete/load-balancer/terragrunt.hcl" }}
  ```

  {{ .Requirements }}

  {{ .Resources }}

  {{ .Inputs }}

  {{ .Outputs }}

output:
  file: README.md
  mode: replace
  template: |-
    <!-- BEGIN_TF_DOCS -->
    {{ .Content }}
    <!-- END_TF_DOCS --> 

sort:
  enabled: true
  by: required

settings:
  anchor: true
  color: true
  default: true
  description: false
  escape: true
  hide-empty: false
  html: true
  indent: 2
  lockfile: true
  required: true
  sensitive: true
  type: true